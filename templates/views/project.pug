extends ../layouts/default
block content
  main.project
    if project
      div
        h1= project.name
        section!= project.description
      img.heroImage(src=project._.heroImage.limit(1600,1400)).img-rounded
    else
      h1 nie ma takiego projektu
